// Прогоняется vitest'ом перед КАЖДЫМ тестовым файлом (vitest.config.ts →
// setupFiles), то есть раньше, чем загрузятся модули самого теста.
//
// Тесты не должны открывать боевую panel.db ни при каких условиях: часть из них
// чистит таблицы (`DELETE FROM audit_log`) и пишет фикстуры. Изоляция стояла в
// самих файлах строкой `process.env.DB_PATH = ':memory:'`, но статические
// import'ы ESM поднимаются выше неё — db.ts успевал прочитать реальный путь, и
// `npm test`, запущенный в контейнере с примонтированной базой, стирал журнал
// аудита. Здесь переменная выставляется до загрузки любого модуля приложения.
process.env.DB_PATH = ':memory:';
